server:
  port: 2222

spring:
  application:
    name: auth-server
  profiles:
    active: dev
#  cloud:
#    config:
#      discovery:
#        enabled: true
#        serviceId: config-server
#      profile: dev
#      fail-fast: true