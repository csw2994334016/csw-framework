server:
  port: 7777

spring:
  application:
    name: points-server
  profiles:
    active: dev
#  cloud:
#    config:
#      discovery:
#        enabled: true
#        serviceId: config-server
#      profile: dev
#      fail-fast: true

