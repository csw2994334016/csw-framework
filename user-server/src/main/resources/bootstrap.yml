server:
  port: 3333

spring:
  application:
    name: user-server
  profiles:
    active: dev
#  cloud:
#    config:
#      discovery:
#        enabled: true
#        serviceId: config-server
#      profile: dev
#      fail-fast: true
