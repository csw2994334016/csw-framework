server:
  port: 9999

spring:
  application:
    name: develop-server
  profiles:
    active: dev
#  cloud:
#    config:
#      discovery:
#        enabled: true
#        serviceId: config-server
#      profile: dev
#      fail-fast: true
